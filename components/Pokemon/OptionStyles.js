import styled from 'styled-components'

export const Option = styled.option`
    color: #fff;
    cursor: pointer;
    line-height: 2;
    font-size: 20px;

    &:hover{
        text-decoration: underline;
        font-weight: 700;
    }
`